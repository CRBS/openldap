######
######   THIS FILE IS MANAGED BY PUPPET!   ######
######
######   If you need a long-term modification to this file, it must be put in SVN in order to persist.
######   Contact CRBS SysOps via http://support.crbs.ucsd.edu if you need assistance.
######

## Managed by puppet  // openldap/templates/etc_pam_ldap.conf.erb
uri <%= @ldap_uri %>
base <%= @openldap_base_dn %>
port 636
timelimit 30
bind_timelimit 30
idle_timelimit 3600
nss_initgroups_ignoreusers root,ldap,named,avahi,haldaemon,dbus,radvd,tomcat,radiusd,news,mailman
tls_cacertfile /etc/pki/tls/certs/ca-bundle.crt
pam_password md5
rootbinddn <%= @openldap_root_dn %>
ssl no
nss_base_passwd ou=People,<%= @openldap_base_dn %>
nss_base_group ou=Group,<%= @openldap_base_dn %>
nss_base_netgroup ou=Netgroup,<%= @openldap_base_dn %>
sudoers_base  ou=SUDOers,<%= @openldap_base_dn %>

